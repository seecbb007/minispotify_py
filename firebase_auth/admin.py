# Used to import Django's admin module. 
# This module provides the functionality for the Django admin interface, 
# which is a built-in feature of Django that allows you to manage your application's data through a web-based interface.
from django.contrib import admin

# Register your models here.
